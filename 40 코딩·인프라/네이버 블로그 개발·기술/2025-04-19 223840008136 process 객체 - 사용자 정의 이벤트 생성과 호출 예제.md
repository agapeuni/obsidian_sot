---
title: "2025-04-19 223840008136 process 객체 - 사용자 정의 이벤트 생성과 호출 예제"
type: blog-archive
status: active
created: 2025-04-19
updated: 2026-08-12
tags:
  - 개발·기술
  - naver-blog/archive
source: "https://blog.naver.com/PostView.naver?blogId=agapeuni&logNo=223840008136&categoryNo=982"
source_blog: agapeuni
source_category: 982
log_no: 223840008136
published: "2025. 4. 19. 22:54"
body_hash: 44c8c57f2ed447c60de1f8083103de00495d4a03084264d0083fbb3907d0d114
visibility: private
rag: exclude
---
# 2025-04-19 223840008136 process 객체 - 사용자 정의 이벤트 생성과 호출 예제

- 원문: https://blog.naver.com/PostView.naver?blogId=agapeuni&logNo=223840008136&categoryNo=982
- 게시일: 2025. 4. 19. 22:54

## 본문

![](https://postfiles.pstatic.net/MjAyNTA0MTlfMTM1/MDAxNzQ1MDcwODMwOTUw.9OuRBPjtr7Ge0bZyjegAN-8X5L0bRW-mCoV-BViLHDUg.XdyWZQOGetmf9r4lxYXhuyQiWpefAnXKsZ2DFFnjLj8g.PNG/Node_js-001_\(4\).png?type=w80_blur)

​

**1\. 개요**

Node.js의 process 객체를 활용하여 사용자 정의 이벤트를 생성하고, 이를 강제로 호출하는 방법에 대해 살펴본다. process 객체는 Node.js에서 전역적으로 사용 가능한 객체로, 이벤트 기반의 비동기 처리를 수행할 수 있도록 EventEmitter를 상속받고 있다. 이를 통해 자체적인 이벤트를 정의하고, 원하는 시점에 호출하는 방식으로 다양한 이벤트 기반 프로그래밍이 가능하다.

​

이번에는 process 객체에 이벤트를 생성하고 강제로 호출해 보는 예제를 진행해 본다.

​

**2\. 설명**

Node.js는 비동기 이벤트 기반 구조를 갖춘 런타임 환경으로, 이벤트를 처리하는 기본 인터페이스인 EventEmitter를 제공한다. 대부분의 Node 객체들은 EventEmitter를 상속받아 이벤트 처리 기능을 내장하고 있으며, process 객체 역시 그 중 하나이다.

​

process 객체는 이벤트를 핸들링하는 EventEmitter의 인스턴스이다. EventEmitter를 상속받아서 process 객체에 이벤트를 생성하고 호출할 수가 있다. 이벤트를 생성할 때 on()과 once() 함수를 사용하여 만든다. on()으로 생성한 이벤트는 제거하기 전까지 계속 사용 가능한데 반해 once()로 생성한 이벤트는 한번 사용되면 제거된다. emit() 메서드는 이벤트를 다른 쪽으로 전달하고 싶을 때 사용한다.

​

이벤트를 정의하기 위해서는 on() 또는 once() 메서드를 사용한다.

  * on() 메서드는 지정된 이벤트가 발생할 때마다 반복적으로 실행되며, 이벤트가 명시적으로 제거되기 전까지 계속 유효하다.

  * once() 메서드는 최초 한 번만 실행되고 이후에는 자동으로 제거된다.

  * emit() 메서드는 정의된 이벤트를 실제로 발생시키는 역할을 하며, 정의된 이벤트 핸들러를 호출한다.

​

이와 같은 구조를 통해 개발자는 명시적으로 원하는 이벤트 흐름을 설계할 수 있다.

​

​

**3\. 예제**

아래는 전체 코드이다.

/** process : instance of EventEmitter */ process.on('myEvent1', function() { console.log('myEvent1 호출'); }); process.emit('myEvent1'); process.emit('myEvent1'); process.emit('myEvent1'); process.once('myEvent2', function() { console.log('myEvent2 호출'); }); process.emit('myEvent2'); process.emit('myEvent2'); process.emit('myEvent2'); var exitEvent = function() { console.log("exitEvent 호출"); }; setTimeout(function() { console.log('1초 후에 시스템 종료'); process.on('exit', exitEvent); }, 1000);

**실행 결과**

myEvent1 이벤트는 on() 메서드로 생성하여 호출할 때마다 계속 로그를 표시한다. 반면 myEvent2 이벤트는 once() 메서드로 생성하여 한번 호출되고 나서 이후에는 반응을 하지 않는다.

myEvent1 호출 myEvent1 호출 myEvent1 호출 myEvent2 호출 1초 후에 시스템 종료 exitEvent 호출

​

![](https://postfiles.pstatic.net/MjAyNjA1MjRfMTkw/MDAxNzc5NjE2MDUxNTU3.gRdnv_P7S3FnhnNsQpBSyqmWFPl74Lp76KD6HCozerEg.xxFmn3g6bTUbMKKh8IJ2zsEr-KhIzG8ciXJSyny78X4g.PNG/%EC%A3%BC%ED%95%9C%EA%B8%B8.png?type=w966)

#Nodejs #process객체 #이벤트처리 #EventEmitter #비동기프로그래밍 #nodejs공부 #자바스크립트기초 #모듈사용법 #이벤트기반 #nodejs강의 #on메서드 #once메서드 #emit메서드 #process이벤트 #코딩예제 #개발자블로그 #백엔드개발 #nodejs모듈 #비동기이벤트 #nodejs입문 #nodejs예제 #웹개발 #자바스크립트개발 #코딩학습 #프로그래밍기초 #코딩블로그 #개발자되기 #자바스크립트학습 #node이벤트 #개발팁

![](https://postfiles.pstatic.net/MjAyNTA0MTlfNTMg/MDAxNzQ1MDcwNDM4OTU2.7mZK7F4v29zFDR09yn6FxqEUWdk98jMthqSJxP9EfU8g.iX3067ru8290-Gd0QeL3Q-F6TYY230lIQALtbwRso8cg.PNG/%ED%95%98%EB%8B%A8_365_%ED%95%9C%EA%B1%B8%EC%9D%8C%EC%94%A9-%EA%BE%B8%EC%A4%80%ED%95%98%EA%B2%8C.png?type=w80_blur)

_다른 곳에 이용하실 때에는 출처를 밝혀 주세요._

_공감과 이웃추가는 좋은 글을 쓰는데 힘이 됩니다._
